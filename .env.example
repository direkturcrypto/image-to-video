# Copy this file to ".env" and fill in your keys. The CLI loads it automatically.
# Precedence: --api-key/--tts-key flags  >  real environment variables  >  .env

# Image generation + LLM (prompts & narration), via derouter
DEROUTER_API_KEY=sk-...

# MiMo v2.5 TTS (token plan)
MIMO_TTS_KEY=tp-...
